
player = {
    x = 400,
    y = 400,
}

process = {
    x,
    y,
    id,
    program = {
        man,
        variable
    },
    draw = love.graphics.rectangle("line", x, y, GLOBAL_WIDTH, GLOBAL_HEIGHT)
}